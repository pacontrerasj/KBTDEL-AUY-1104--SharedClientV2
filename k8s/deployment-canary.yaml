apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-api-canary
  labels:
    app: demo-api
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-api
      version: canary
  template:
    metadata:
      labels:
        app: demo-api
        version: canary
    spec:
      containers:
        - name: api
          image: blackrosse/demo-api:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
